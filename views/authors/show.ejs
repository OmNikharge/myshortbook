<h2>
    <%= author.name %>
</h2>
<a href="/authors/<%= author.id %>/edit ">Edit</a>
<%- include('../partials/deleteform',{url: `/authors/${author.id}`}) %>
    <% if(book.length>0){ %>
        <h2>Books By Author <%= author.name %>
        </h2>
        <div>
            <% book.forEach(book=> { %>
                <a href="/books/<%= book.id %>">
                <img height="150" width="100" src="<%= book.coverImagePath %>"></a>
                <% }) %>
        </div>
        <% } %>
        